# Visora AI

一个面向 `cc-relay + HyperFrames` 的在线视频自动生成工作台原型。

## 功能

- 输入主题并自动提炼成 brief
- 自动生成视频大纲和场景列表
- 生成 cc-relay 请求体预览
- 生成 HyperFrames 配置预览
- 支持主题优化、场景追加、项目重命名、JSON 导出

## 运行

直接用浏览器打开 `index.html` 即可。

## 说明

这是一个静态 MVP 原型，当前前端完成了完整交互闭环，后续可以把 `cc-relay` 接口和 HyperFrames 渲染工作流接到真实后端。
